{
  "$schema": "https://railway.com/railway.schema.json",
  "build": { "builder": "RAILPACK" },
  "deploy": {
    "startCommand": "python -m pip install --no-cache-dir email-validator==2.2.0 && uvicorn app.main:app --host 0.0.0.0 --port 8000"
  }
}
